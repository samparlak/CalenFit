/**
 * Config Module Barrel Export
 * 
 * Provides clean import paths for configuration:
 * import { APP_CONFIG, FEATURE_FLAGS } from '@/config'
 */

export {
    APP_CONFIG,
    SEO_CONFIG,
    THEME_CONFIG,
    FEATURE_FLAGS,
    API_CONFIG,
    EXTERNAL_SERVICES,
    VALIDATION_CONFIG,
    ANIMATION_CONFIG,
    LAYOUT_CONFIG,
    default as config,
} from './app.config'
